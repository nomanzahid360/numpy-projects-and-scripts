import numpy as np

arr = np.array([1, 2, 3, 4, 5, 6])

# Aggregation Functions
print("Maximum :", np.max(arr))
print("Minimum :", np.min(arr))
print("Mean :", np.mean(arr))
print("Standart Daviation :", np.std(arr))
print("Variance :", np.var(arr))
print("Sum :", np.sum(arr))
